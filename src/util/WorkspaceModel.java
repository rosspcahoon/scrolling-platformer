package util;

import viewUtil.Renderable;

/**
 * The WorkspaceModel is an abstraction of the object being altered by
 * the user. For example, in SLogo, a Room was a Workspace. In the level 
 * editor, a Level (being edited) is a workspace.
 * @author Dagbedji Fagnisse
 *
 */
public abstract class WorkspaceModel implements Renderable{

}
